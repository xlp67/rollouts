app:
  image:
    repository: "pramsey/pg_tileserv"
    registry: registry.hub.docker.com/
    tag: latest
  imagePullSecrets:
    enabled: false
  logRoot: "/var/log"
  envVars:
    TS_HTTPPORT: 7800
  envVarsRaw:
    - name: DATABASE_URL
      valueFrom:
        secretKeyRef:
          name: hasura
          key: pg-db-url

appName: agent-territory-management-tiles

http:
  app:
    ports:
      - name: http
        containerPort: 7800
    readinessProbe:
      tcpSocket:
        port: 7800
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 2048Mi

  # priorityClassName: internal-frontend-important

nginx:
  enabled: false

service:
  name: tiles
  targetPort: http
  type: NodePort
